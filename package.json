{
  "name": "my-budget-app",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n \"API,UI\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:ui": "npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
